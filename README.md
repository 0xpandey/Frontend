# Interactive Question Management Sheet

A comprehensive web application for managing hierarchical question structures.

**Author:** Om Pandey

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
  